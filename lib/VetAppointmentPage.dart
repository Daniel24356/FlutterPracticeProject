import 'dart:io';

import 'package:flutter/material.dart';

class VetAppointmentPage extends StatelessWidget {
  const VetAppointmentPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
